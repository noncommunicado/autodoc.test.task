Made By:		Kutumov Nikita
Email:		nik_2008@bk.ru
Telegram:		https://t.me/hamaronooo (@hamaronooo)
Date:			2022-07-18
Time Spend:		~13-15h
Tech:			.NET6 & MSSQL Server Express
Libs:			Automapper, MediatoR, EF Core with migrations, Serilog, ...


Description:

	🔸 В целом ничего особенного в проекте нет:
	- Использован MediatoR для реализации CQRS.
	- EF Core для миграции и доступа к БД.
	- Automapper для автомаппинга (😁) entities на dto.
	- Выбрал хранить файлы именно на диске, только ради того, чтобы бэкапы БД не растягивались на часы, т.к. в задаче указано, что файлы могут весить много.
		Наверное, можно было сделать распределенно, небольшие файлы в БД, а большие на диск, однако смысла в этом не вижу, с диска читает быстрее.

	🔸 Структура:
	- src/core/ домен и application layer 
	- src/infrastructure/ сервисы работающие с внешней средой
	- src/presentation/ Web API

	🔸 Чтобы я еще сделал, но не в рамках тестового задания:
	- Добавил бы Dapper, но только на чтение, опервции записи и обновления оставляем на плечах EF Core.
	- Написал бы юнит тесты для некоторых сервисов, тестировать контроллеры в рамках такой скромной логики не вижу смысла.
	- Возможно стоит обратиться к паттерну репозиторий, чтобы сделать еще более модно, слоёно и слабосвязно.
	- Реализовал возможность сжимать файлы перед их записью на диск, уходило бы существенно дольше на CRUD, но экономили бы место (опционально и для больших файлов).
	- Авторизация JWT + Refresh Token.

Notes:

	- Если захотите запустить, не забудьте поменять Connection String в appsettings.json проекта autodoc.tasks.api
		Скорее всего достаточно поменять только сервер, база создастся автоматически (можно не накатывать миграцию)
	- Тестовые данные создадутся автоматически при первом запуске, чтобы их создать еще раз, проще всего удалить базу (ну или truncate table)
	- persistence инжектится через метод расширения (см. DependencyInjection.cs)
	- Я выбрал передачу файлов через IFormFile, т.к. подумал, что это не файловый обменник и искать обходные пути для улучшения унивесальности и производительности
		наверное бессмысленно, тем более, что мобильные приложения умеют работать с веб-формами))

Конец